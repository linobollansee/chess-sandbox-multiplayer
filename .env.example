# Example Environment Configuration File
# Copy this file to .env and replace the values with your actual configuration

# PostgreSQL Database Connection URL (optional)
# If not set, the application will run without database persistence
# Format: postgresql://username:password@host:port/database
# Example: postgresql://postgres:mypassword@localhost:5432/chessdb
DATABASE_URL=

# Server Port (optional, defaults to 3000)
# PORT=3000
